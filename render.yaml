services:
  - type: web
    name: deacons-database
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATA_DIR
        value: /data
    disk:
      name: deacons-data
      mountPath: /data
      sizeGB: 1
